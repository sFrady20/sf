use bun as the package manager
make all comments in my tone, simple, aloof, not explanatory, just for readability, mostly lower case, no space after slashes, somewhat cheeky
i will most likely have the dev server running so do not run the build command unless you really need to because they both will break
